/*
 * Juan
 * 3/14/25
 * 
 * #1: a), because you can't instantiate an interface directly in Java.
 * #2: b), because the Resizable interface does define the resize method, so
 * calling resize(2.5) is valid.
 */